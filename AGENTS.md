# OfferLens — 项目指令

## 每次改动之后（强制，两条都要做）

### 1. 提交

每完成一次文件改动，立即 `git commit`。不要把多个不相关的改动攒成一个 commit。

- 前缀用 `feat:` / `fix:` / `chore:` / `style:` / `docs:` / `ci:` / `refactor:`
- **格式化必须单独成 commit**，语义改动另算——这样 `git log -p` 才能一眼区分两者
- 提交前先 `git status`，确认没有把不该入库的东西 stage 进去
- commit message 写"为什么改"，不写"改了什么文件"
- **push / 建 tag / 改远程可见设置属于对外动作，每次都要先取得用户同意**，不因之前授权过就当作长期许可

### 2. 清理

每次改动收尾时删掉本轮产生的不必要的临时文件，不要留到会话结束。

- 判断依据：**当前代码/文档是否还需要读它**。仍作为证据被引用的（如端到端运行的事件流、报告存档）要保留，并保证其在 `.gitignore` 的忽略范围内（本仓用 `.tmp_*` 前缀）
- 已过期的一次性产物要删：失败的运行日志、中间态草稿、被替换掉的手工脚本、空文件
- **只删自己本轮创建的文件。** 发现来历不明的文件时先问，不要顺手删
- 后台任务跑完后停掉遗留进程与监控任务
- `node_modules/`、`.offerlens/`、`coverage/` 属于工具管理的产物，不用手工清，也别误删

## 验证纪律

- **改动过的代码，验证必须穿过被改的那条路径。** 绕开它的测试必然绿，那是假信号
- 新增门槛/守卫时，先把它设成一个必然失败的值、确认命令真的红，再改回实测值
- 不声称未测量的数字。量化陈述要能被当前工作树上的一条命令复现；引用他人测量时注明出处与日期
- 跨工具大版本的指标**不可直接比较**（例：vitest 3→4 改了 branch/function 的计数定义），
  口径变化要点名指出，不能让数字看起来像进步或退步

## 依赖与配置

- 互相有精确 peer 要求的依赖（如 `vitest` 与 `@vitest/coverage-v8`）**锁精确版本**，不用 `^`
- 修改 `package.json` 后必须同步 `package-lock.json`，并实跑一次 `npm ci`
- 配置文件里被工具"静默忽略"的写法是缺陷不是自由：写前先确认该键在当前版本真的生效
